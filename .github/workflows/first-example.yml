name: my first workflow

on:
  push:
    branches:
      - testpipeline

jobs: 
  first_job:
    runs-on: ubuntu-latest
    steps:
    - name: welcome message
      run: echo "my first github action job"

    - name: List files
      run: ls
    
    - name: Read files
      run: cat ../../README.md




